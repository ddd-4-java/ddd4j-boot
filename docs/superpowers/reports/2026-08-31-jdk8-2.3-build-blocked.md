# JDK 8 `2.3.x` 构建阻塞记录

- 日期：2026-08-31
- 分支：`2.3.x`
- 基线 SHA：`bf10bae867ac017ac1f75a0aca88cb0390a6939d`
- JDK：Amazon Corretto `1.8.0_504`
- Maven：`3.9.16`
- 执行命令：`JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -q -DskipTests install`

## 结果

构建在 `ddd4j-boot-core` 的编译期依赖解析阶段失败，尚未进入自动配置或生命周期测试。

```text
Could not find artifact com.github.hiwepy:mybatis-plus-enhance:jar:2.7.x.20251205-SNAPSHOT
in 2624322-snapshot-3EoOv3
```

依赖由 `ddd4j-boot-dependencies/pom.xml` 的 `mybatis-plus-enhance.version` 声明，并由 `ddd4j-boot-core/pom.xml` 消费。本地 Maven 缓存只有 `.lastUpdated` 标记，没有 JAR 或 POM。

## 影响与恢复条件

- 状态：`BLOCKED`，影响 `2.3.x / CORE_SPI_REPOSITORY_CQRS` 的真实 JDK 8 构建和后续契约测试。
- 未以较新 JDK、不同坐标或本地伪造构件绕过该门禁。
- 恢复条件：将该精确 SNAPSHOT 发布到已声明候选仓库，或经单独确认修正 JDK 8 兼容的依赖版本并验证其行为等价。

# JDK 8 `2.3.x` 构建阻塞记录

- 日期：2026-08-31
- 分支：`2.3.x`
- 基线 SHA：`bf10bae867ac017ac1f75a0aca88cb0390a6939d`
- JDK：Amazon Corretto `1.8.0_504`
- Maven：`3.9.16`
- 执行命令：`JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -q -DskipTests install`

## 结果

初次构建在 `ddd4j-boot-core` 的编译期依赖解析阶段失败，尚未进入自动配置或生命周期测试。

```text
Could not find artifact com.github.hiwepy:mybatis-plus-enhance:jar:2.7.x.20251205-SNAPSHOT
in 2624322-snapshot-3EoOv3
```

依赖由 `ddd4j-boot-dependencies/pom.xml` 的 `mybatis-plus-enhance.version` 声明，并由 `ddd4j-boot-core/pom.xml` 消费。初始本地 Maven 缓存只有 `.lastUpdated` 标记，没有 JAR 或 POM。

## 本地恢复验证

已从 `mybatis-plus-enhance` 的已核验 Java 8 源分支 `origin/2.7.x`（`441c10571c83a1c3d68af6fb1dc07dec51fd0e0c`）构建其版本更新后的精确本地构件：`2.7.x.20260630-SNAPSHOT`。`ddd4j-boot 2.3.x` 的依赖管理已同步到此 revision（提交 `75906b5e`），并在 Corretto 8 下通过：

```text
JAVA_HOME=$(/usr/libexec/java_home -v 1.8) mvn -q -DskipTests install
```

这仅证明源码和本地构件可构建；候选仓库中尚未可解析该 SNAPSHOT，因此发布门禁仍为 `BLOCKED`。

## 影响与恢复条件

- 状态：`BLOCKED`，影响候选仓库可解析性和发布就绪；本地 JDK 8 编译已恢复，但核心逻辑契约测试尚未补齐。
- 未以较新 JDK、不同坐标或伪造二进制构件绕过该门禁；本地构件来自已核验的 JDK 8 源分支。
- 恢复条件：将 `2.7.x.20260630-SNAPSHOT` 的精确构件发布到已声明候选仓库，并以干净 Maven 缓存重试构建。
